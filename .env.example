# Environment Configuration for Survey Data System
# Copy this file to .env and fill in your actual values

# Database Connection (supports both DB_* and standard PostgreSQL PG* variables)
# You can use either naming convention - the app checks both
DB_HOST=your-project.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-password-here

# Alternative: PostgreSQL Standard Environment Variables
# PGHOST=your-project.supabase.co
# PGPORT=5432
# PGDATABASE=postgres
# PGUSER=postgres
# PGPASSWORD=your-password-here

# Flask Application Security
# CRITICAL: Change this in production to a strong random value
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production

# OpenAI API Configuration (Optional - Required for AI/ML features)
# Used for: AI-powered text analysis, embeddings generation, intelligent categorization
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here
